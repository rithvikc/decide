<div class = "container">
  <h1 class = "mb-5">All your Current Events</h1>
  <% @events.each do |event| %>
    <h3><%= event.name %></h3>
    <p><%= event.description %></p>
    <p>at <%= event.start_at %></p>
    <% end %>
</div>
